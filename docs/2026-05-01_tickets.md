# 2026-05-01 Tickets

## Ticket 1

Title: Implement Microfilm table contract types and validation rules

Body:

**Summary**

Add backend contract types for the Miller Showcase Microfilm table so the API can represent columns, regular rows, custom rows, scalar cell values, write requests, and JSON error envelopes.

**Scope**

- Add `Outermind\Microfilm\TableTypes.cs`.
- Define `MicrofilmTableColumn`, `MicrofilmTableRow`, `MicrofilmCellValue`, `MicrofilmTableErrorEnvelope`, and supporting constants/rules.
- Support frontend-facing scalar cell JSON values through `MicrofilmCellValueJsonConverter`.
- Validate column IDs, duplicate columns, supported column types, positive widths, dropdown options, row cells, and type-specific cell values.
- Ensure cell values follow the frontend contract: strings/null for `text`, finite numbers/null for `number`, dropdown option strings/null for `dropdown`, and booleans for `checkbox`.

**Acceptance Criteria**

- `MicrofilmTableColumn` supports `id`, `name`, `type`, `dropdownOptions`, and `width`.
- `MicrofilmTableRow` supports `id`, `origin`, and `cells`.
- Cell values serialize as raw JSON scalars, not wrapper objects.
- Invalid column schemas produce machine-readable error codes such as `INVALID_COLUMN_ID`, `DUPLICATE_COLUMN_ID`, `UNSUPPORTED_COLUMN_TYPE`, and `INVALID_COLUMN_WIDTH`.
- Invalid cell values produce `INVALID_CELL_VALUE`.
- Default custom-row cells initialize as `null` for text/number/dropdown and `false` for checkbox.

**Metadata**

**Type**: Feature  
**Point Estimate**: 4

## Ticket 2

Title: Add Microfilm table commands and events

Body:

**Summary**

Add durable Totem commands and events for table schema replacement, regular-row updates, custom-row creation, custom-row updates, seeding, and expected rejection outcomes.

**Scope**

- Add `Outermind\Microfilm\TableCommands.cs`.
- Add `Outermind\Microfilm\TableEvents.cs`.
- Define commands including `SeedMicrofilmTable`, `ReplaceMicrofilmTableColumns`, `CreateMicrofilmRegularRow`, `UpdateMicrofilmRegularRowCell`, `CreateMicrofilmCustomRow`, and `UpdateMicrofilmCustomRowCell`.
- Define accepted events including `MicrofilmTableSeeded`, `MicrofilmTableColumnsChanged`, `MicrofilmRegularRowCreated`, `MicrofilmRegularRowCellUpdated`, `MicrofilmCustomRowCreated`, and `MicrofilmCustomRowCellUpdated`.
- Define rejection events including `MicrofilmTableClientNotRecognized`, `MicrofilmTableColumnNotRecognized`, `MicrofilmTableRowNotRecognized`, `MicrofilmTableColumnSchemaRejected`, `MicrofilmTableCellValueRejected`, and `MicrofilmTableRowConflict`.

**Acceptance Criteria**

- Every write command includes enough context to route and validate by `clientId`.
- Accepted write events include authoritative columns or full row payloads for API responses and query projections.
- Rejection events include enough context for JSON error envelopes.
- Seed commands include `clientId`, `seedId`, columns, and regular rows.

**Metadata**

**Type**: Feature  
**Point Estimate**: 2

## Ticket 3

Title: Implement client-routed Microfilm table topic

Body:

**Summary**

Create a Totem topic that owns per-client Miller Showcase table decisions, validation, seeding, regular rows, custom rows, and write outcomes.

**Scope**

- Add `Outermind\Microfilm\Topics\MicrofilmTableTopic.cs`.
- Route table commands by `clientId`.
- Recognize clients from `ClientCreated` and `ClientReassigned`.
- Apply seed data idempotently without overwriting existing table state.
- Replace full column schemas while preserving submitted order.
- Create and update generic regular rows independently from Box/Roll domain state.
- Create and update custom rows separately from regular rows.
- Reconcile row cells when schema changes.
- Emit accepted or rejected events for every table write.

**Acceptance Criteria**

- Unknown client writes emit `MicrofilmTableClientNotRecognized`.
- Duplicate/invalid columns emit `MicrofilmTableColumnSchemaRejected`.
- Unknown row writes emit `MicrofilmTableRowNotRecognized`.
- Unknown column writes emit `MicrofilmTableColumnNotRecognized`.
- Invalid cell values emit `MicrofilmTableCellValueRejected`.
- Successful row updates emit the full updated row.
- Successful custom-row creation emits a backend-issued stable row ID.
- Seed commands are idempotent and do not reset edited table state.

**Metadata**

**Type**: Feature  
**Point Estimate**: 8

## Ticket 4

Title: Add Microfilm table read models

Body:

**Summary**

Add Totem queries that project known clients, table columns, regular rows, and custom rows into API-ready envelopes.

**Scope**

- Add `Outermind\Microfilm\Queries\MicrofilmClientLookupQuery.cs`.
- Add `Outermind\Microfilm\Queries\MicrofilmTableColumnsQuery.cs`.
- Add `Outermind\Microfilm\Queries\MicrofilmRegularRowsQuery.cs`.
- Add `Outermind\Microfilm\Queries\MicrofilmCustomRowsQuery.cs`.
- Track known server IDs and client IDs for API validation.
- Project `columns`, `rows`, and `customRows` from table events.
- Ensure known clients with no data return empty arrays.

**Acceptance Criteria**

- `MicrofilmClientLookupQuery` can confirm known servers and clients.
- `MicrofilmTableColumnsQuery` returns `{ columns: [] }` for known clients with no configured schema.
- `MicrofilmRegularRowsQuery` returns rows with `origin: "regular"`.
- `MicrofilmCustomRowsQuery` returns rows with `origin: "custom"`.
- Row projections are updated after schema changes and cell update events.
- Query shapes are compatible with Totem camelCase JSON serialization.

**Metadata**

**Type**: Feature  
**Point Estimate**: 4

## Ticket 5

Title: Wire Microfilm table HTTP APIs and error envelopes

Body:

**Summary**

Expose the Miller Showcase table contract under `/api/microfilm` and return JSON error envelopes for expected selector/table errors.

**Scope**

- Update `Outermind.Web\Controllers\MicrofilmController.cs`.
- Add `Outermind.Web\Controllers\MicrofilmTableApiErrors.cs`.
- Add `GET /api/microfilm/columns/{clientId}`.
- Add `PUT /api/microfilm/columns/{clientId}`.
- Add `GET /api/microfilm/rows/{clientId}`.
- Add `PATCH /api/microfilm/rows/{clientId}/{rowId}`.
- Add `GET /api/microfilm/custom-rows/{clientId}`.
- Add `POST /api/microfilm/custom-rows/{clientId}`.
- Add `PATCH /api/microfilm/custom-rows/{clientId}/{rowId}`.
- Update `GET /api/microfilm/clients/{serverId}` so unknown servers return `404 UNKNOWN_SERVER`.
- Validate client existence before table reads.
- Return authoritative `{ columns }` and `{ row }` payloads from write responses.

**Acceptance Criteria**

- Known clients return `200` for columns, rows, and custom rows.
- Known empty states return empty arrays, not `404`.
- Unknown table clients return `404` with `UNKNOWN_CLIENT`.
- Unknown selector server IDs return `404` with `UNKNOWN_SERVER`.
- Column replacement returns `{ columns }`.
- Regular row PATCH returns `{ row }`.
- Custom row POST returns `201` with `{ row }`.
- Custom row PATCH returns `{ row }`.
- Expected errors use `{ "error": { "code", "message", "details" } }`.

**Metadata**

**Type**: Feature  
**Point Estimate**: 8

## Ticket 6

Title: Add durable demo seed support for Miller Showcase

Body:

**Summary**

Add service-host support for emitting configured table seed commands so the current Miller demo client has starter columns and a regular row in durable timeline state.

**Scope**

- Add `Outermind.Service\MicrofilmTableSeedOptions.cs`.
- Add `Outermind.Service\MicrofilmTableSeedService.cs`.
- Update `Outermind.Service\ServiceCollectionExtensions.cs` to register `MicrofilmTableSeedService`.
- Update `Outermind.Service\appsettings.json` with `Microfilm:TableSeeds` for the Miller demo client.
- Seed client `e83ddf61-1dd2-4584-8def-762338863a59` with starter columns and row `row-1`.
- Keep configuration as a seed trigger only; durable timeline events are the source of truth after seeding.

**Acceptance Criteria**

- Service startup can emit `SeedMicrofilmTable` commands from configuration.
- The Miller demo client is configured with columns `boxName`, `rollName`, `status`, and `reviewed`.
- The Miller demo client is configured with one regular row containing `Box 01`, `Roll A`, `New`, and `false`.
- Seed commands are idempotent through `MicrofilmTableTopic`.
- Known clients without seed data still return empty arrays.

**Metadata**

**Type**: Feature  
**Point Estimate**: 4

## Ticket 7

Title: Add Microfilm table topic and query tests

Body:

**Summary**

Add focused tests for table seeding, schema validation, row updates, custom row creation, query projections, and known-client lookup.

**Scope**

- Add `tests\Quantum.Tests\MicrofilmTableTests.cs`.
- Cover `MicrofilmTableTopic` accepted and rejected outcomes.
- Cover `MicrofilmTableColumnsQuery`, `MicrofilmRegularRowsQuery`, `MicrofilmCustomRowsQuery`, and `MicrofilmClientLookupQuery`.
- Verify seeded schemas and rows project correctly.
- Verify custom row defaults and backend-issued IDs.
- Verify invalid dropdown values and duplicate columns are rejected.

**Acceptance Criteria**

- New Microfilm table tests pass independently.
- Tests prove known clients with no columns return empty schema state.
- Tests prove successful row updates return and project full rows.
- Tests prove custom rows initialize missing cells with type defaults.
- Tests prove client lookup tracks known server and client IDs.

**Metadata**

**Type**: Improvement  
**Point Estimate**: 4

## Ticket 8

Title: Document frontend integration handoff for Miller Showcase table APIs

Body:

**Summary**

Create a frontend-facing integration guide that explains how to consume the new Microfilm table APIs, including endpoint contracts, validation rules, edge cases, and QA guidance.

**Scope**

- Add `docs\MILLER_SHOWCASE_FRONTEND_HANDOFF.md`.
- Document selector flow and successful response shapes.
- Document backend decisions for schema storage, regular row mapping, custom row persistence, and demo seed data.
- Document all table read/write endpoints.
- Document scalar cell value rules and error envelope format.
- Document frontend best practices for optimistic rows, authoritative write responses, ETags, schema changes, and last-write-wins behavior.
- Document QA edge cases and live smoke-test steps.

**Acceptance Criteria**

- The guide explicitly resolves backend open questions from the original handoff.
- The guide explains the seeded Miller demo client.
- The guide includes examples for `columns`, `rows`, `customRows`, and write responses.
- The guide lists known error codes and expected HTTP statuses.
- The guide gives frontend-specific integration and QA guidance.

**Metadata**

**Type**: Documentation  
**Point Estimate**: 2

## Ticket 9

Title: Validate Microfilm table implementation and record baseline test status

Body:

**Summary**

Build and test the Microfilm table implementation, then record the validation outcome and known unrelated baseline failure.

**Scope**

- Build `Outermind.Service\Quantum.Service.csproj`.
- Build `Outermind.Web\Quantum.Web.csproj`.
- Run focused Microfilm table tests in `tests\Quantum.Tests\Quantum.Tests.csproj`.
- Run the full `Quantum.Tests` suite and compare failures to baseline.
- Track the pre-existing `ClientProfileRegistryTests.MatchesCorrectProfile` failure separately from the new Microfilm table work.

**Acceptance Criteria**

- `dotnet build .\Outermind.Service\Quantum.Service.csproj --no-restore --nologo --verbosity:minimal` passes.
- `dotnet build .\Outermind.Web\Quantum.Web.csproj --no-restore --nologo --verbosity:minimal` passes.
- New Microfilm table tests pass independently.
- Full `Quantum.Tests` has no new failures beyond the known pre-existing `ClientProfileRegistryTests.MatchesCorrectProfile` failure.
- Validation results are included in the implementation handoff.

**Metadata**

**Type**: Improvement  
**Point Estimate**: 2
